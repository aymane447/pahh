{% extends 'base.html.twig' %}

{% block title %}Ajouter un message{% endblock %}

{% block body %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Ajouter</button>
   <a href="{{ path('app_mess') }}" class="btn btn-secondary">Retour à la liste des messages</a>
{{ form_end(form) }}
{% endblock %}