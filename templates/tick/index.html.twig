{% extends 'base.html.twig' %}

{% block title %}Tickets - TicketFlow{% endblock %}

{% block body %}
<div class="container py-5">
    <!-- Header Section -->
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-4 mb-5">
        <div>
            <h1 class="display-5 fw-bold mb-1 text-white">Tickets</h1>
            <p class="text-white-50 mb-0">Gérez et suivez vos demandes de support.</p>
        </div>
        {% if is_granted('ROLE_AGENT') or is_granted('ROLE_ADMIN') %}
            <a href="{{ path('app_tick_ajou') }}" class="btn btn-primary px-4 py-2 rounded-pill shadow-sm">
                <i class="fas fa-plus me-2"></i> Nouveau Ticket
            </a>
        {% endif %}
    </div>

    <!-- Filters Section -->
    <div class="glass-card mb-5 p-4 rounded-4 shadow-lg">
        <form action="{{ path('app_tick') }}" method="GET" class="row g-3 align-items-end">
            <div class="col-lg-4 col-md-6">
                <label class="form-label">Recherche</label>
                <div class="input-group">
                    <span class="input-group-text bg-transparent border-end-0" style="border-radius: 12px 0 0 12px; border-color: rgba(255,255,255,0.1);">
                        <i class="fas fa-search text-muted"></i>
                    </span>
                    <input type="text" name="q" value="{{ filters.q }}" class="form-control border-start-0" style="border-radius: 0 12px 12px 0;" placeholder="Titre, description...">
                </div>
            </div>
            <div class="col-lg-2 col-md-6">
                <label class="form-label">Catégorie</label>
                <select name="category" class="form-select">
                    <option value="">Toutes</option>
                    {% for cat in categories %}
                        <option value="{{ cat.id }}" {{ filters.category == cat.id ? 'selected' }}>{{ cat.nom }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="col-lg-2 col-md-6">
                <label class="form-label">Statut</label>
                <select name="statut" class="form-select">
                    <option value="">Tous</option>
                    <option value="Ouvert" {{ filters.statut == 'Ouvert' ? 'selected' }}>Ouvert</option>
                    <option value="En cours" {{ filters.statut == 'En cours' ? 'selected' }}>En cours</option>
                    <option value="Résolu" {{ filters.statut == 'Résolu' ? 'selected' }}>Résolu</option>
                </select>
            </div>
            <div class="col-lg-2 col-md-6">
                <label class="form-label">Priorité</label>
                <select name="priorite" class="form-select">
                    <option value="">Toutes</option>
                    <option value="Basse" {{ filters.priorite == 'Basse' ? 'selected' }}>Basse</option>
                    <option value="Moyenne" {{ filters.priorite == 'Moyenne' ? 'selected' }}>Moyenne</option>
                    <option value="Haute" {{ filters.priorite == 'Haute' ? 'selected' }}>Haute</option>
                </select>
            </div>
            <div class="col-lg-2 d-flex gap-2">
                <button type="submit" class="btn btn-primary flex-grow-1 rounded-pill">Filtrer</button>
                {% if filters.q or filters.category or filters.statut or filters.priorite %}
                    <a href="{{ path('app_tick') }}" class="btn btn-outline-secondary rounded-circle p-2 d-flex align-items-center justify-content-center" style="width: 42px; height: 42px;" title="Réinitialiser">
                        <i class="fas fa-undo"></i>
                    </a>
                {% endif %}
            </div>
        </form>
        <div class="mt-3 small text-white">
            <i class="fas fa-list-ul me-2"></i> {{ tickets|length }} ticket(s) trouvé(s)
        </div>
    </div>

    <!-- Tickets List Table -->
    <div class="card overflow-hidden border-0 shadow-lg rounded-4 glass-card" style="padding: 0;">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0" style="background: transparent !important;">
                <thead style="background: rgba(255, 255, 255, 0.03) !important; color: #f8fafc !important; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1.5px; border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;">
                    <tr>
                        <th class="ps-4 py-4" style="border: none !important; background: transparent !important;">ID</th>
                        <th class="py-4" style="border: none !important; background: transparent !important;">Titre & Description</th>
                        <th class="py-4 text-center" style="border: none !important; background: transparent !important;">Statut</th>
                        <th class="py-4" style="border: none !important; background: transparent !important;">Priorité</th>
                        <th class="py-4" style="border: none !important; background: transparent !important;">Créé le</th>
                        <th class="pe-4 py-4 text-end" style="border: none !important; background: transparent !important;">Actions</th>
                    </tr>
                </thead>
                <tbody style="border: none !important; background: transparent !important;">
                    {% for t in tickets %}
                    <tr class="ticket-row" style="cursor: pointer; transition: all 0.2s; background: transparent !important; border-bottom: 1px solid rgba(255, 255, 255, 0.05) !important;" onclick="window.location.href='{{ path('app_tick_show', {'id': t.id}) }}'">
                        <td class="ps-4" style="border: none !important; background: transparent !important;">
                            <span class="badge rounded-pill px-3 py-2" style="background: rgba(56, 189, 248, 0.1); color: #38bdf8; font-weight: 700;">#{{ t.id }}</span>
                        </td>
                        <td style="min-width: 300px; border: none !important; background: transparent !important;">
                            <div class="d-flex align-items-center gap-2">
                                <span class="fw-bold text-white">{{ t.titre }}</span>
                                {% if t.hasUnreadMessagesFor(app.user) %}
                                    <span class="badge bg-danger rounded-pill pulse-animation">Nouveau</span>
                                {% endif %}
                                {% if t.event %}
                                    <span class="badge bg-info bg-opacity-10 text-info rounded-pill" style="font-size: 0.7rem;">
                                        <i class="fas fa-ticket-alt me-1"></i> Événement
                                    </span>
                                {% endif %}
                            </div>
                            <div class="small text-white-50 text-truncate" style="max-width: 250px;">{{ t.description|slice(0, 60) }}...</div>
                        </td>
                        <td class="text-center" style="border: none !important; background: transparent !important;">
                            {% set status_styles = {
                                'Ouvert': 'background: rgba(16, 185, 129, 0.1); color: #10b981; border: 1px solid rgba(16, 185, 129, 0.2);',
                                'En cours': 'background: rgba(245, 158, 11, 0.1); color: #f59e0b; border: 1px solid rgba(245, 158, 11, 0.2);',
                                'Résolu': 'background: rgba(148, 163, 184, 0.1); color: #94a3b8; border: 1px solid rgba(148, 163, 184, 0.2);'
                            } %}
                            {% set current_style = status_styles[t.statut] ?? 'background: rgba(255, 255, 255, 0.05); color: #94a3b8;' %}
                            <span class="badge rounded-pill px-3 py-2 text-uppercase fw-bold" style="{{ current_style }} font-size: 0.65rem; letter-spacing: 0.5px;">
                                {{ t.statut }}
                            </span>
                        </td>
                        <td style="border: none !important; background: transparent !important;">
                            {% set priority_color = t.priorite|lower == 'haute' ? '#ef4444' : (t.priorite|lower == 'moyenne' ? '#f59e0b' : '#0ea5e9') %}
                            <div class="d-flex align-items-center gap-2 fw-semibold" style="font-size: 0.85rem; color: {{ priority_color }};">
                                <i class="fas fa-circle" style="font-size: 0.5rem;"></i>
                                {{ t.priorite }}
                            </div>
                        </td>
                        <td style="border: none !important; background: transparent !important;">
                            <div class="small text-white-50">{{ t.dateCreation ? t.dateCreation|date('d M Y') : 'N/A' }}</div>
                        </td>
                        <td class="pe-4 text-end" style="border: none !important; background: transparent !important;" onclick="event.stopPropagation();">
                            <div class="d-flex justify-content-end gap-2">
                                <a href="{{ path('app_tick_show', {'id': t.id}) }}" class="action-btn" title="Voir">
                                    <i class="far fa-eye text-white-50" style="font-size: 0.9rem;"></i>
                                </a>
                                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_AGENT') %}
                                    <a href="{{ path('app_tick_mod', {'id': t.id}) }}" class="action-btn" title="Modifier">
                                        <i class="far fa-edit text-primary" style="font-size: 0.9rem;"></i>
                                    </a>
                                    <a href="{{ path('app_tick_sup', {'id': t.id}) }}" class="action-btn" onclick="return confirm('Supprimer ce ticket ?')" title="Supprimer">
                                        <i class="far fa-trash-alt text-danger" style="font-size: 0.9rem;"></i>
                                    </a>
                                {% endif %}
                                {% if is_granted('ROLE_CLIENT') and t.idClient is null %}
                                     <a href="{{ path('app_tick_acheter', {'id': t.id}) }}" class="btn btn-sm btn-warning fw-bold px-3 rounded-pill">Acheter</a>
                                {% endif %}
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="6" class="py-5 text-center text-white-50" style="border: none !important; background: transparent !important;">
                            <div>
                                <i class="fas fa-inbox fa-3x mb-3 opacity-20"></i>
                                <p>Aucun ticket trouvé.</p>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    .glass-card {
        background: rgba(255, 255, 255, 0.03) !important;
        backdrop-filter: blur(10px) !important;
        -webkit-backdrop-filter: blur(10px) !important;
        border: 1px solid rgba(255, 255, 255, 0.05) !important;
    }
    
    .ticket-row {
        background: transparent !important;
    }

    .ticket-row:hover {
        background-color: rgba(255, 255, 255, 0.03) !important;
    }

    .action-btn {
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(255, 255, 255, 0.05) !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 8px;
        text-decoration: none;
        transition: all 0.2s;
    }

    .action-btn:hover {
        background: rgba(255, 255, 255, 0.1) !important;
        transform: translateY(-2px);
        border-color: rgba(255, 255, 255, 0.2) !important;
    }
    
    .pulse-animation {
        animation: pulse 2s infinite;
    }
    
    @keyframes pulse {
        0% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7); }
        70% { transform: scale(1); box-shadow: 0 0 0 10px rgba(239, 68, 68, 0); }
        100% { transform: scale(0.95); box-shadow: 0 0 0 0 rgba(239, 68, 68, 0); }
    }
    
    /* Force transparency on all table elements to inherit glass background */
    .table-responsive table, 
    .table-responsive thead, 
    .table-responsive tbody, 
    .table-responsive tr, 
    .table-responsive td, 
    .table-responsive th {
        background: transparent !important;
        background-color: transparent !important;
    }
</style>
{% endblock %}
